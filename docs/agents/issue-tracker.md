# Issue Tracker

**Tracker:** GitHub Issues — repo `darkhorsefiji/vanuarai`

Issues are created and managed via the `gh` CLI. Skills that read/write issues
(`to-issues`, `triage`, `to-prd`, `qa`) use `gh issue create` / `gh issue list`.

**External PRs as a request surface:** No.
